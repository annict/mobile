import { StackNavigator } from "react-navigation"

import { SeasonalWorksScreen } from "../containers/works/screens"

export const SeasonalWorksStackNavigator = StackNavigator({
  Indexx: {
    screen: SeasonalWorksScreen,
  },
})
